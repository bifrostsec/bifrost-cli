# Bifrost CLI

A command-line tool for uploading SBOM (Software Bill of Materials) files to Bifrost Security Platform.

## Usage

```bash
./bifrost sbom upload /path/to/sbom.json --api-token <token>
```